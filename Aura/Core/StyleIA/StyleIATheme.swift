import SwiftUI

enum StyleIATheme {
    static let deepBlack = Color(hex: 0x000100)
    static let panel = Color(hex: 0x071008)
    static let surfaceGreen = Color(hex: 0x101B12)
    static let moss = Color(hex: 0x6EA369)
    static let sage = Color(hex: 0xA4BE9F)
    static let gold = Color(hex: 0xC4A45F)
    static let text = Color.white.opacity(0.93)
    static let subtleText = Color.white.opacity(0.34)
    static let faintText = Color.white.opacity(0.18)
    static let hairline = Color.white.opacity(0.1)
}
